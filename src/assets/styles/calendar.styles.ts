import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%',
  },
  taskListContainer: {
    width: '100%',
    marginTop: 20,
    borderRadius: 10,
    padding: 15,
  },
  noTasksText: {
    marginTop: 20,
    textAlign: 'center',
    fontSize: 16,
  },
});
